---
import Head from "@components/Head.astro";
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";
import { SITE } from "@consts";

type Props = {
  title: string;
  description: string;
  image?: string;
};

const { title, description, image } = Astro.props;
---

<!doctype html>
<!--
 ┌───────────────────────────────────────────────────────┐
 │                                                       │
 │                ________________________               │
 │                    __                                 │
 │                    / |    /                           │
 │                ---/__|---/----__----__-               │
 │                  /   |  /   /   ) /   )               │
 │                _/____|_/___(___(_/___/_               │
 │                                                       │
 │                                                       │
 │   __________________________________________________  │
 │       _   _            __                             │
 │       /  /|          /    )   ,               ,       │
 │   ---/| /-|----__---/-------------__----__-------__-  │
 │     / |/  |  /   ' /  --,   /   /   ) /   ) /   (_ `  │
 │   _/__/___|_(___ _(____/___/___/___/_/___/_/___(__)_  │
 │                                                       │
 │                                                       │
 └───────────────────────────────────────────────────────┘

 Created with Monodraw
 Website based on: https://github.com/markhorn-dev/astro-nano
-->
<html lang="en">
  <head>
    <Head title={`${title} | ${SITE.NAME}`} description={description} image={image} />
  </head>
  <body>
    <Header />
    <main class="flex-1 py-32">
      <slot />
    </main>
    <Footer />
  </body>
</html>
